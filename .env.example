VITE_APP_NAME=DistribuHub

# Server only. Omit for the isolated local PostgreSQL from pnpm db:setup.
# Set to an existing PostgreSQL database for deployment; never prefix with VITE_.
# DATABASE_URL=postgresql://user:password@localhost:5432/marketplace_demo
# ALLOW_DEMO_SEED=true
